[pytest]
DJANGO_SETTINGS_MODULE = seu_projeto.settings
python_files = test_.py tests.py test_*.py *_tests.py *_test.py
addopts = 
    --doctest-modules
    --strict-markers
markers = 
    slow: Run tests that are slow
    fast: Run fast tests